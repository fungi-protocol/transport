[package]
name = "fungi-wire"
description = "Canonical typed messages and grow-only message sets for Fungi"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
sha2 = "0.10"

[dev-dependencies]
fungi-transport = { path = "../transport" }
hex = "0.4"
proptest = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }

[lints]
workspace = true
